# Core dependencies
# Gradio is intentionally NOT pinned here: the Space's Gradio SDK runtime
# (sdk_version in README.md) provisions its own gradio install, and also
# listing a gradio== pin in requirements.txt conflicts with that and breaks
# the build.
setuptools>=65.0.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
audioread>=3.0.0

# Video processing
opencv-python-headless>=4.8.0
mediapipe>=0.10.0

# Scientific computing
numpy>=1.24.0,<2.0.0
scipy>=1.11.0

# Utilities
python-dotenv>=1.0.0
